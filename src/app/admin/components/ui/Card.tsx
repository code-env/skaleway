import React from "react";

const Card = () => {
  return (
    <div className="flex-1 p-10 rounded-lg bg-neutral shadow-inner transition-all duration-300 cursor-pointer">
      Card
    </div>
  );
};

export default Card;
